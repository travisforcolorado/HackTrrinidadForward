import React from "react";
import ReactDOM from "react-dom/client";
import TrinidadHelpFinder from "./TrinidadHelpFinder.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TrinidadHelpFinder />
  </React.StrictMode>
);
